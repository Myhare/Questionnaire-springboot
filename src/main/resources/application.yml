spring:
  application:
    name: questionnaire_backstage
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: defaultDataSource
    password: 123456
    url: jdbc:mysql://localhost:3306/questionnaires?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
  devtools:
    restart:
      enabled: true
  resources:
    static-locations: classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/,file:${web.upload-path}
  # 邮件相关配置
  mail:
    username: 1940307627@qq.com
    password: yxnmblbfwvjndibh  # 发送邮件的密钥
    host: smtp.qq.com
    # qq邮箱需要开启加密验证
    properties:
      mail:
        smtp:
          ssl:
            enable: true

server:
  port: 8888

web:
  # 绑定邮箱前端地址
  bandEmailHost: http://localhost:8080/#/user/bandEmail/
  # 服务器路径
  # upload-path: /home/uploadedImg/
  # 本地路径
  upload-path: F:/Java_IDEA练习/questionnaire/uploadFile/
  # 获取头像路径
  get-head-path: /uploadFile/

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      # 逻辑删除：1表示已删除 0表示未删除
      logic-delete-value: 1
      logic-not-delete-value: 0
  mapper-locations: classpath:/mapper/**/*.xml
